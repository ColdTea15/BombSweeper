/**
 * The `Driver` class serves as the entry point for the BombSweeper game.
 * It initializes the game board and starts the game.
 */
public class Driver
{
	public static void main(String[] Args)
	{
		GameBoard b = new GameBoard("BombSweeper", 20,20);
	}
}
